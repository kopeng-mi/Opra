// Assertions for the encounter predictor: the SOI crossing against a brute-force scan, the
// post-encounter conic, and the no-crossing case. Returns the failure count.
#pragma once

namespace opra::orbit {

int encounter_tests();

}  // namespace opra::orbit
