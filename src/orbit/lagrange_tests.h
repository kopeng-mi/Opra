// Assertions for the Lagrange module: the five points from the mass ratio, and the co-rotating
// placement sim/system.cpp's propagate gives a station with a Lagrange seat. Returns failures.
#pragma once

namespace opra::orbit {

int lagrange_tests();

}  // namespace opra::orbit
