// Module tests for the world's berthing path: src/sim/world_tests.cpp.
#pragma once

namespace opra {

/** Approach, capture, hard dock, undock, refusal. Returns the failure count. */
int world_tests();

}  // namespace opra
