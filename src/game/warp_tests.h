// Module tests for the warp rail: src/game/warp_tests.cpp.
#pragma once

namespace opra {

/** Rail steps, the railed boundary and the automatic drops. Returns the failure count. */
int warp_tests();

}  // namespace opra
